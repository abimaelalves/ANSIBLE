pipeline {
  agent any

  stages {
    stage('Hello') {
      steps {
        sh '''
          ansible --version
          ansible-playbook --version
          ansible-galaxy --version
          cat /var/jenkins_home/workspace/ansible/ansible.cfg
        '''
      }
    }
  }
}