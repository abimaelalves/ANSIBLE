pipeline {
  agent any

  stages {
    stage('Hello') {
      steps {
        sh '''
          ansible --version
          ansible-playbook --version
          ansible-galaxy --version
          cat /var/jenkins_home/workspace/ansible/ansible/ansible.cfg
          ansible 192.168.50.10 -m ping -v 
        '''
      }
    }
  }
}